<% ptype = item %>


<%= link_to ptype.to_s, do_sample_problem_path(ptype.id), remote: :true %>

  
<%= link_to_function "Create", "custom_problem_form(#{ptype.id}, \"#{ptype}\")", :class => "pull-right" %>
