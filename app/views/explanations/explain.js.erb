if($('#problem_overlay .explanation').length < 1) {
	<% if @orig_prob.problem.is_a?(QuestionWithExplanation) %>
		$('#problem_overlay .problem').after('<%= j(render "explanation") %>');
		showExplanation();
	<% else %>
		alert('no explanation');
	<% end %>
}
