<%= form_for [user, topic], :url => user_topics_path(user), remote: :true do |f| %>
  <% if topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, 'Name' %>
    <%= f.text_field :title%>
  </div>
    <%= f.label :description, 'Content', :id => 'top_tit' %>
  <%=render 'shared/wysihtml5'%>
  <section>
  <div class="field">
    <%= f.text_area :description%>
  </div>
  </section>
<script>
wysi('topic_description', '#top_tit');
</script>
  
  <div class="field">
    <%= f.hidden_field :classroom_id, :value => classroom_id %>
  </div>

  <div class="actions">
    <%= f.submit 'Publish Post'%>
  </div>
<% end %>