<div class="row">
	<%#= @student.name %>
	<% if @student.is_a?(Student) and current_user.is_a?(Teacher) %>
 		<%= link_to "Give a Merit", new_teacher_badge_path(current_user, :student_id => @student.id), remote: :true, :class => 'button-blue', id: "merit_link_#{@student.id}" if @merit.blank? %>
    <% end %>
    <div class "row" id="merit_box_<%= @student.id %>">
        <%= render "badges/student_merit", :user_id => @student.id, :merit => @merit %>
    </div>
    <div class"row" id="merit_form_<%= @student.id %>" style="display: inline-block;">
    </div>
</div>
<div id="problems_history" class="teacher-details-header" style="margin-bottom:5px;"></div>
<div class="row">
  	<div class="col-md-4">
  		<div class='cols-his'>
	  	<h2>Problem Sets</h2>
	  	<ul>
			<% @problem_sets.each do |problem_set| %>
				<li><%= link_to problem_set.name, get_problem_set_history_path(@student.id, problem_set_id: problem_set.id), remote: :true %></li>
			<% end %>
	  	</ul>
	  </div>
  	</div>
  	<div class="col-md-4">
		<div id="problem_types_column_list">
		</div>
	</div>
	<div class="col-md-4">
		<div id="problems_column_list">
		</div>
	</div>
</div>
