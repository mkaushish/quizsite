<%= content_for :head do %>
  <%= javascript_include_tag "nondefault/drawtools" %>
<% end %>
<div class="logo">Drawing Tools for Geometry</div>
<p class=desc>The following app has a compass and straightedge which students use for geometry problems.  See if you can bisect the line on the screen using these tools!<p>
<div id="tools">
  <div id="protractor" >Protractor</div>
  <div id="line" >Line</div>
  <div id = "compass" class=selected>
    <span>Compass</span>
    <input type="checkbox" id="usecirclesize">
    <input type="text" id="circlesize">
  </div>
  <!-- button id="ruler">Ruler</button -->
  <div id="clear" >Clear</div>
</div>
<canvas id="canvas" height="400" width="550"> 
Your browser does not support the HTML5 Canvas feature. This app uses new HTML5 features and will only work on the latest versions of Firefox, Safari or Chrome (and maybe Internet Explorer 9).         
</canvas>   
<div id="shapes"></div>
<% shapes = ["circle:200:200:50", "line:40:50:300:325"] %>
<%= hidden_field_tag('startshapes', "line:#{@x1}:#{@y1}:#{@x2}:#{@y2}")   %>
<%= hidden_field_tag 'geometry', "" %>
