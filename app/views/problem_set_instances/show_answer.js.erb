<% if callback %>
	<%= render partial: callback %>
<% end %>
initProblemOverlay();

// create the problem
$('#problem_overlay').html('<%= j(render "answers/full_answer") %>');
$("#points").html("<%= j render("students/student_points", :@student => @student) %>");
initProblem();

