<div class="row-fluid">
  	<%= render 'teacher_info', :teacher => @teacher %>
  	<h3 style="display:inline-block;">Overview:</h3>
</div>
<div class="row-fluid">
	<div class="dotted-border pull-right">
		<%= link_to "Assign Quiz to all", new_quiz_for_all_path %> <br />
		<%= link_to "Assign Quiz with All Problem Types", new_quiz_with_all_ptypes_path %>
	</div>
</div>
<% @classrooms.each do |classroom| %>
	<%= render :partial => 'classrooms/overview', :locals => {:classroom => classroom} %>
<% end %>
<script>
  	$(function () {
    	$('.teacher-class-col').jScrollPane();
    	$('.class_password').tooltip({title: "Class Password: students need this to join your classroom"});
  	})
</script>